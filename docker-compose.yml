services:
  app:
    image: ${APP_IMAGE:-syndichain:latest}
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - ./.env.app          # on server: ~/syndichain/.env.app
    # REDIS_URL must be set in .env.app pointing to your cloud Redis
